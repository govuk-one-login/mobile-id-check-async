{
  "name": "helper-scripts",
  "version": "1.0.0",
  "description": "ID Check helper scripts",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/.bin/tsc",
    "start": "node dist/generateHashedSecret/index",
    "generateHashedSecret": "npm run build --silent && npm run start --silent"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^12.5.0",
    "zx": "^8.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.7.3"
  }
}
