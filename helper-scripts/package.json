{
  "name": "helper-scripts",
  "version": "1.0.0",
  "description": "Scripts for deploying, updating and deleting AWS SAM stacks",
  "main": "index.js",
  "type":"module",
  "scripts": {
    "build": "./node_modules/.bin/tsc",
    "start": "node dist/index.js",
    "manageStacks": "npm run build --silent && npm run start --silent",
    "playground": "npm run build --silent && node ./dist/playground.js --silent",
    "format": "./node_modules/.bin/prettier --write '**/*.ts'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^13.1.0",
    "inquirer": "^12.4.1",
    "zx": "^8.3.0"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  }
}
